import React from 'react';
import TopBar from '../Components/ComponentsBar/TopBar';

const Thread = () => {
    return (
        <div>
            <TopBar />
        </div>
    );
};

export default Thread;