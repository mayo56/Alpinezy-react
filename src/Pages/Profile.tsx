import React from 'react';
import ProfileController from '../Components/ProfileComponents/ProfileController';

const Profile = () => {
    return (
        <div>
            <ProfileController/>
        </div>
    );
};

export default Profile;
