import React from 'react';
import { Liste } from '../../../Thread/ThreadController';

const Messages = (props:{guild:Liste|null}) => {
    return (
        <div>
            
        </div>
    );
};

export default Messages;